
Day 1
I created the next.js app, then I edited the heading. After that I practices the typescript drills where I did the types and functions as instructed.

I have build a product list page, with added functionality like searching, sorting, and adding an extra column for selecting the wanted quantity

Day 2
I have done the parts needed for the assigned day 4, 
The server component serves ready HTML to the browser to display instantly, non dynamic stuff, like routing between different pages is not dynamic, no need to remember the href value

And the client component is used to allow the browser to remember data, like useState when data changes the browser sees these adn changes and remembers them so that the can be referenced.

